extends layouts/main

block content
    .form-container
        form(action=`/users${routes.deleteVideo}`, method="post")
            input(type="text", placeholder="Title")
            input(type="text", placeholder="Description")
            input(type="submit", value="Update Video")
        a.form-container__link(href=`/user${routes.deleteVideo}`) Delete Video